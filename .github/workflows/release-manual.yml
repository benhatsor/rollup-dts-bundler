name: Manual release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    uses: ./.github/workflows/release.yml
    with:
      is-auto: false
    permissions:
      contents: write
      id-token: write