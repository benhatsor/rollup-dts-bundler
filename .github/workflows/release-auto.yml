name: Automatic release

on:
  push:
    branches:
      - main

jobs:
  release:
    if: github.actor == 'renovate[bot]'
    uses: ./.github/workflows/release.yml
    with:
      is-auto: true
    permissions:
      contents: write
      id-token: write